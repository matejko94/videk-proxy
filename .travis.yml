language: python
python:
  - "2.7"
install:
  - pip install requests
  - pip install numpy
  - pip install pymongo
services:
  - mongodb
before_script:
  - sudo bash videk-proxy start
  - sleep 5
script: python proxy-client.py
